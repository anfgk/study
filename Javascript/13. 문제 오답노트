13. 문제 오답노트
JavaScript에서 값과 데이터 타입을 모두 엄격하게 비교하여 일치할 때만 `true`를 반환하는 연산자는 무엇일까요?
1. !=
2. =
3. ===
4. == // 값만 비교하고, 자료형이 달라도 형변환 후 비교
정답: 3번
내가 고른 답: 4

JavaScript 모듈에서 `export default`를 사용하여 내보낸 항목을 가져올 때, `import` 구문에서 중괄호 `{}` 없이 원하는 이름으로 가져올 수 있는 이유는 무엇일까요?
1. 항상 고정된 이름으로만 가져와야 해서
2. 해당 모듈의 '기본값'으로 내보내졌기 때문에
3. 상수만 내보낼 때 사용하는 방식이라서
4. 여러 항목을 동시에 가져오기 위해서 // 이건 named export와 관련된 설명임 ({ A, B })
정답: 2번
내가 고른 답: 4번